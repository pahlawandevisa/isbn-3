{
  "name": "isbn",
  "version": "1.0.0",
  "description": "This module contains a function to check an ISBN is valid or not",
  "main": "src/index.js",
  "scripts": {
    "lint": "eslint src",
    "test": "istanbul cover node_modules/mocha/bin/_mocha -- -R spec --recursive --timeout 5000"
  },
  "author": "BaoNQ",
  "license": "MIT",
  "devDependencies": {
    "chai": "^3.5.0",
    "eslint": "^3.5.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.0.2"
  }
}
